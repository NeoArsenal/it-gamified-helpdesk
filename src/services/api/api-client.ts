/**
 * Central API Client - Re-exports modularized API domains for backward compatibility.
 * All functions are split into domain-specific modules inside `src/services/api/`.
 */
export * from './index';
